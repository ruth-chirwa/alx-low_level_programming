#include <stdio.h>
/**
 * main - Entry point
 *
 * puts - This is a function that writes a line of output to the screen
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
